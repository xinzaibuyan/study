semget函数的key值如何获取：可以通过ftok函数获取。但是呢这个key具有唯一性就好，就像身份证号一样，可辨识一些。然后呢ftok可以获取文件设备的编号和节点，这是唯一的。
还有，在这种消息队列，共享内存、信号量创建之时，需要给***get函数的第三个参数IPC_CREAT|***
但是如果已经创建了的话，仅仅是用于获取id，可以在第三个参数设置为0。
三个通信方式的创建都使用一个key，就是共同控制一个区域。
整体流程，首先创建一片共享内存，然后通过消息队列，告诉你此时你是需要读还是写，最后通过信号量，保证数据在同一时间只有一个进程在读或者写。